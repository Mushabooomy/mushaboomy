import { base } from "@theme-ui/presets";

const theme = {
  ...base,
  containers: {
    page: {
      width: "100%",
      maxWidth: "960px",
      m: 0,
      mx: "auto",
    },
  },
  styles: {
    ...base.styles,
  },
};

export default theme;
